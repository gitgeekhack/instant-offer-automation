[Unit]
Description=Docker Compose Application Service
After=network.target

[Service]
Type=simple
Restart=always
WorkingDirectory=/home/ubuntu/instant-offer-automation
ExecStart=/bin/bash -c '/usr/bin/docker-compose up'

[Install]
WantedBy=multi-user.target
